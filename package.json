{
  "name": "scrimba-moody-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:vercel": "vercel --prod",
    "deploy:netlify": "netlify deploy --prod",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "firebase": "^12.7.0"
  }
}
